# Optional / heavy dependencies — installed best-effort in Docker.
# If any of these fail, the API still starts; only the endpoints under
# /models/* that use them will return a 503 with a clear error message.

# --- Statistics / econometrics ---
statsmodels>=0.14.0
arch>=6.2.0                   # GARCH

# --- Portfolio optimization ---
PyPortfolioOpt>=1.5.5
cvxpy>=1.3.0

# --- Deep learning & RL (lazily imported by models/ml, models/rl, models/nlp) ---
torch>=2.0.0
stable-baselines3>=2.1.0
gymnasium>=0.29.0

# --- Data plumbing ---
pyarrow>=14.0.0
pandas-datareader>=0.10.0
beautifulsoup4==4.12.2

# --- Visualization (used by notebooks/ + report templates) ---
plotly>=5.18.0

# --- Reporting (templates/) ---
markdown==3.4.4
jinja2==3.1.2
weasyprint==59.0
pypandoc==1.12
python-pptx==0.6.23
reportlab==4.0.7

# --- Scheduling (automation/) ---
schedule>=1.2.0

# --- Utilities ---
tqdm==4.66.1

# --- Notebook stack (optional dev/tutorial use) ---
jupyter>=1.0.0
jupyterlab>=4.0.0
ipywidgets>=8.1.0
notebook>=7.0.0

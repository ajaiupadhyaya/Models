# Frontend build-time env for split-host deploys.
# Copy to `.env.production` (or set in Vercel project env vars).
# Leave empty when serving the SPA from the same FastAPI origin.

# The full origin of the backend (no trailing slash):
VITE_API_ORIGIN=https://models-terminal.fly.dev

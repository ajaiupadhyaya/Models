# API runtime dependencies — minimal set required for `python -m uvicorn backend.main:app`
# to import and serve cleanly. Heavy ML/RL libs live in requirements.txt
# (best-effort install in Dockerfile) and are loaded lazily by /models endpoints.

# --- Web framework ---
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
pydantic>=2.7.0
python-multipart>=0.0.27
websockets>=16.0          # asyncio support for openai / yfinance
httpx>=0.27,<0.29         # compatible with current fastapi.TestClient

# --- Auth & config ---
PyJWT>=2.10.0
python-dotenv>=1.0.1

# --- Data layer ---
sqlalchemy>=2.0.0
psycopg2-binary>=2.9.9
alembic>=1.13.0

# --- Background jobs ---
# APScheduler is the v1 default (in-process, no broker). Celery+Redis remain for v2.
apscheduler>=3.10.0
celery[redis]>=5.3.0
redis>=5.0.0

# --- Numerical / data core (needed at module-load by api & core) ---
numpy>=1.26.0,<3
pandas>=2.2.0
scipy>=1.11.0
scikit-learn>=1.3.0
python-dateutil>=2.8.2
pytz>=2023.3

# --- Market & macro data sources ---
yfinance>=0.2.40
fredapi>=0.5.1
alpha-vantage>=2.3.1

# --- AI clients ---
openai>=1.0.0
anthropic>=0.18.0

# --- Excel / spreadsheets ---
openpyxl>=3.1.2
xlsxwriter>=3.1.2

# --- News sentiment (lightweight, no model download) ---
vaderSentiment>=3.3.2

# --- HTTP & misc ---
requests>=2.33.0
fuzzywuzzy>=0.18.0
python-Levenshtein>=0.21.0
